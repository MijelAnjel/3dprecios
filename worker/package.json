{
  "name": "dprecios-forum-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute forum-db --local --file=./schema.sql",
    "db:migrate:prod": "wrangler d1 execute forum-db --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
